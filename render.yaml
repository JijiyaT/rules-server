services:
  - type: web
    name: rules-engine
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true
    plan: free